{
  "name": "pr-to-notion",
  "version": "1.0.0",
  "type": "module",
  "description": "Auto-generate PR study notes and push to Notion",
  "main": "sync.js",
  "scripts": {
    "sync": "node --openssl-legacy-provider sync.js",
    "preinstall": "export NODE_OPTIONS=--openssl-legacy-provider"
  },
  "dependencies": {
    "dotenv": "^16.0.0",
    "node-fetch": "^3.3.0"
  }
}